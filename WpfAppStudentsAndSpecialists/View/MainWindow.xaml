<Window x:Class="WpfAppStudentsAndSpecialists.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppStudentsAndSpecialists"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="5">
            <ListView ItemsSource="{Binding Students}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding FirstName}" Width="100"/>
                        <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding LastName}" Width="100"/>
                        <GridViewColumn Header="Индекс группы" DisplayMemberBinding="{Binding IdGroup}" Width="100"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="5">
            <TextBlock Text="Индекс"/>
            <TextBox Text="{Binding IndexGroup}"/>
            <Button Content="Получить список" Command="{Binding ShowListStudentsByIndexGroup}"/>
            <Button Content="Очистить" Command="{Binding ClearList}"/>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Мальчиков:" Margin="3"/>
                <TextBlock Text="{Binding Male}"  Margin="3"/>
                <TextBlock Text="Девочек:"  Margin="3"/>
                <TextBlock Text="{Binding Female}"  Margin="3"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
            <Button Content="Перейти к группам"/>
        </StackPanel>



    </Grid>
</Window>
